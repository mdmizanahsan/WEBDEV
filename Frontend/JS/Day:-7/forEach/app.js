   let arr = [1,2,3,4,5];

    let print = function(el) {
        console.log(el);
   }

     // call
     arr.forEach(print);  // 

            // or
        
     let arr1 = [1,2,3,5,4];

       arr.forEach((el) => {
        console.log(el);
       }
    );